export async function executeCommand(el, condition, options = {}, params = []) {
    const result = await condition(el, ...params, options);
    return {
        el,
        success: result.result === true,
        values: result.value
    };
}
